# Electrs Configuration (not used by default; prefer CLI flags in compose)
network = bitcoin
electrum_rpc_addr = "0.0.0.0:50001"
daemon_rpc_addr = "bitcoind:8332"
daemon_p2p_addr = "bitcoind:8333"
cookie = "${BITCOIN_RPC_USER}:${BITCOIN_RPC_PASSWORD}"
db_dir = "/data"